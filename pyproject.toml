[project]
name = "apogee-manager"
version = "5.0.0"
description = "Local dashboard and account manager for Google Antigravity IDE"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Reyand"}
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
